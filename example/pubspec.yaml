name: example
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=2.18.4 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  retrofit: ^3.3.1
  cupertino_icons: ^1.0.2
  freezed_annotation: ^2.0.3
  dio: ^4.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0
  retrofit_generator: ^4.2.0
  build_runner: ^2.3.2
  json_serializable: ^6.5.4
  freezed: ^2.1.0+1
  mocked_backend: ^1.0.6

flutter:
  uses-material-design: true